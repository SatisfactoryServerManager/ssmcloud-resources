syntax = "proto3";

option go_package = "github.com/SatisfactoryServerManager/ssmcloud-resources/proto/generated";
import "models/empty.proto";

service AgentStateService {
  rpc UpdateAgentStateStream (stream AgentStateRequest) returns (SSMEmpty);
  rpc UpdateAgentState (AgentStateRequest) returns (SSMEmpty);
}

message AgentStateRequest {
    bool online = 1;
    bool installed = 2;
    bool running = 3;
    float cpu = 4;
    float ram = 5;
    int64 installedSFVersion = 6;
    int64 latestSFVersion = 7;
}